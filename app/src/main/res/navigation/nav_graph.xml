<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_fragment_list">

    <fragment
        android:id="@+id/navigation_fragment_list"
        android:name="com.example.reignapp.ListFragment"
        tools:layout="@layout/fragment_list">
        <action
            android:id="@+id/navigation_action_list_to_web_view"
            app:destination="@id/navigation_fragment_web_view" />
    </fragment>

    <fragment
        android:id="@+id/navigation_fragment_web_view"
        android:name="com.example.reignapp.WebViewFragment"
        tools:layout="@layout/fragment_web_view" />
</navigation>